# PulseIQ — Requirements
# ========================
# Install:  pip install -r requirements.txt
#
# macOS M2 Apple Silicon:
#   PyTorch includes MPS support by default — no special index needed.
#   Ensure macOS 12.3+ and Python 3.11+

# ── REST API ──────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.7.0

# ── ML core ───────────────────────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
scikit-learn>=1.4.0
sentence-transformers>=2.7.0
torch>=2.2.0
transformers>=4.40.0
umap-learn>=0.5.6
hdbscan>=0.8.33

# ── Data & HTTP ───────────────────────────────────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
apscheduler>=3.10.0
feedparser>=6.0.10
rapidfuzz>=3.0.0
yfinance>=0.2.0
cachetools>=5.3.0
slowapi>=0.1.0
sse-starlette>=2.0.0
tenacity>=8.2.0

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.22.0
kaleido>=0.2.1
tqdm>=4.66.0

